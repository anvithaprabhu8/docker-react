sudo: required
services:
  - docker

before_install:
  -  docker build -t anvithaprabhu8/docker-react_a -f dockerfile.dev . 

script:
  - docker run -e CI:true  anvithaprabhu8/docker-react_a npm run test
